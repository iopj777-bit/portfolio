---
id: cho-gain-portfolio
name: 조가인 포트폴리오
category: portfolio
direction: "Toss의 부드러운 라운드·친근한 신뢰감 구조 + 베이지·화이트 베이스에 코랄 포인트"
reference_inspiration: [toss, claude]
omd: "0.1"
tokens:
  colors:
    ink: "#1C1712"      # 본문/타이틀 텍스트 (웜 블랙)
    accent: "#B8511A"   # 포인트 컬러 — 피치/코랄 계열 (링크, 태그, 활성 상태, 강조 텍스트). 사용자 레퍼런스 #FFB98D의 진한 버전(AA 대비 확보)
    paper: "#FBF8F2"    # 페이지 배경 — 웜 파치먼트
    mist: "#F0E9DA"     # 보조 배경 — 웜 샌드 (칩, 카드 채움)
    line: "#E4DBC9"      # 헤어라인 보더
    muted: "#7C7368"     # 보조 텍스트 — 웜 그레이
  secondary_text:
    - { hex: "#453E34", use: "카피 강조 텍스트 (구 #3E424C)" }
    - { hex: "#534A3D", use: "설명 텍스트 (구 #4A4E58)" }
    - { hex: "#2E2820", use: "진한 본문 텍스트 (구 #2C2F38)" }
    - { hex: "#635947", use: "메타 텍스트 (구 #5A5E68)" }
    - { hex: "#8C8171", use: "플레이스홀더/뱃지 텍스트 (구 #6C6F78, #6E7280)" }
    - { hex: "#D9CEBB", use: "연한 보더/닷 (구 #C7CAD3)" }
    - { hex: "#A99C88", use: "연한 아이콘/보조 텍스트 (구 #9AA0AC)" }
  typography:
    family:
      ui: "Pretendard"
      mono: "JetBrains Mono"
    note: "한글 가독성과 기존 에디토리얼 구조를 유지하기 위해 Pretendard를 그대로 사용. Toss/Claude의 폰트를 그대로 가져오지 않음."
  radius:
    pill: 999
    card: 12–20
  motion:
    note: "기존 opacity/transform 트랜지션 유지 (과도한 모션 지양)"
---

# 조가인 포트폴리오 — 디자인 시스템

## 방향
- **구조/톤**: Toss 스타일의 부드러운 라운드, 넉넉한 여백, 친근하면서 신뢰감 있는 인터랙션(hover opacity, pill 버튼)을 유지.
- **컬러**: 베이지·화이트(파치먼트) 베이스 위에 채도 높은 **딥 코랄** 포인트 하나만 사용. 파랑(Toss 등 핀테크/공공 톤)은 배제하고, 베이지 베이스로 차분함을 잡되 포인트는 또렷하게 살려 포트폴리오다운 임팩트를 냄. 파스텔만 쓰면 힘이 없어 보인다는 피드백에 따라, 포인트 컬러의 채도는 낮추지 않고 명도만 낮춰 "튀지 않으면서도 또렷한" 코랄로 조정.
- **왜 이 조합인가**: 공공·금융 도메인 서비스 기획자 포트폴리오라 신뢰감이 최우선이지만, 파랑 일색인 톤과는 차별화하고 싶어함. 베이지 베이스는 따뜻하고 절제된 인상을, 코랄 포인트는 "번역가/가교"라는 포지셔닝에 어울리는 생기와 임팩트를 더함.

## 접근성
- 포인트 컬러(`#C1472E`)는 흰/파치먼트 배경 대비 약 5:1로 본문 크기 텍스트(태그, 링크)에도 안전하게 사용 가능 (WCAG AA 통과).
- 장식용으로 더 밝은 톤이 필요할 때는 `rgba(193,71,46, x)`를 사용해 같은 색상 계열을 유지.
- 포인트 컬러는 하나만 유지 — 여러 색을 섞지 않는다는 원칙을 지킴 (사용자 명시적 요청).

## 적용 범위
- `index.html`의 CSS 커스텀 프로퍼티(`--ink`, `--accent`, `--paper`, `--mist`, `--line`, `--muted`)와 하드코딩된 보조 텍스트 색상을 전부 웜 뉴트럴 계열로 교체.
- 레이아웃 구조, 타이포 스케일, 컴포넌트 패턴(피pill 태그, 목업 카드, 모달)은 변경하지 않음 — 컬러 시스템만 교체.
